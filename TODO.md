# TODO

## MCP: Postman MCP server setup
- [ ] Check whether `blackbox_mcp_settings.json` already exists; create it if missing.
- [ ] Install Postman MCP server package (`@postman/postman-mcp-server`).
- [ ] Configure `blackbox_mcp_settings.json` to use server name `github.com/postmanlabs/postman-mcp-server`.
- [ ] Start the MCP server in local Minimal mode.
- [ ] Demonstrate one MCP tool capability (e.g., list tools) and record the output.
- [ ] Run project sanity check (npm run lint / build) to ensure no breakage.

